# 이어받기

다른 기기에서 클론해 작업을 이어갈 때 먼저 읽는 문서입니다. 2026-09-01 기준.

브랜치는 `management-screens`. `main`이 아니라 이 브랜치에서 이어갑니다.

---

## 1. 지금 상태

### 돌아가는 것

| 영역 | 상태 |
|---|---|
| 채팅·RAG | 배포됨 |
| 워크플로우 (그래프 편집기, 버전, `@` 호출) | 배포됨 |
| 관리 화면 (분류·사용자·프롬프트·MCP·고급 설정·문서) | 배포됨 |
| 문서 업로드·삭제 | 배포됨 |
| 문서 성격 자동 판정 + 계층 청킹 | 배포됨 |
| 법령 여덟 건 색인 | 배포됨 |

마이그레이션 head는 `0014`.

### 코퍼스 (라이브 DB)

```
유사상품 심사기준          9,070
기술분야별 심사실무가이드   3,651
특허·실용신안 심사기준      2,578
상표심사기준               1,638
지식재산권 권리회복            98
법령 8건 (특허법·상표법·실용신안법 + 시행령·시행규칙)  2,669
```

특허·실용신안 심사기준은 화면에서 `self_contained`로 **재정의**되어 있습니다. 계층 재단하면
recall이 0.923 → 0.769로 떨어져서 되돌린 것입니다. 되돌리지 마십시오.

### 배포 설정

`app_settings` 테이블:

```
RETRIEVAL_TOP_N            8
RETRIEVAL_CANDIDATE_LIMIT  10
WEAK_EVIDENCE_RRF_SCORE    0.0170
QUERY_EXPANSION_COUNT      3   (재시도 전용, 첫 패스는 확장하지 않음)
```

`CANDIDATE_LIMIT`은 10에서 올리지 마십시오. 20에서 시스템이 상품류 목록을 **지어냈고**
인용은 0이었습니다. 측정 기록은 `docs/superpowers/plans/`에 있습니다.

### 현재 지표

| 그룹 | n | recall | anchor |
|---|---|---|---|
| 52문항 (기본) | 52 | 0.885 | **0.904** |
| crossref | 25 | 0.720 | **0.880** |
| statute | 14 | 0.786 | 0.786 |
| colloquial (구어체) | 10 | 0.400~0.500 | 0.100 |

```bash
python scripts/eval_retrieval.py                                   # 기본 52문항
python scripts/eval_retrieval.py --fixture scripts/eval_questions_ko_colloquial.json
python scripts/eval_retrieval.py --retry 3                         # 배포 형태
python scripts/eval_retrieval.py --show crossref                   # 그룹별 슬롯 내역
```

---

## 2. 다음 작업: 근사 중복 청크 제거

두 개의 서로 다른 실패가 **같은 원인**으로 좁혀졌습니다.

- **`등록대상` 질문** — sparse 팔이 `제34조(보정의 각하결정)` **사본 열 개**로 채워집니다.
  그래서 두 팔이 겹칠 수가 없고, 첫 패스 융합 점수가 `0.0164`(= 1/61, dense 1위 하나)로
  기준값 `0.0170`에 걸립니다. **구조적으로 첫 패스를 통과할 수 없습니다.** 답이 나오는지
  여부가 재시도 질의를 LLM이 어떻게 쓰느냐에만 달려 있어서 실행마다 결과가 바뀝니다.
- **`류` 질문** — 답은 맞는데 **인용 표시를 하나도 안 답니다**(`cited=0`). 거의 같은 청크
  여섯 개가 전달되어서입니다.

즉 근사 중복이 슬롯을 독점하는 것이 두 실패의 공통 원인입니다.

### 프롬프트 예시

아래를 그대로 던지면 됩니다.

> 근사 중복 청크가 검색 슬롯을 독점하는 문제를 고쳐줘. 두 실패의 같은 원인이야.
>
> 1. `등록대상은 뭘로 기재해?` — sparse 팔이 `제34조(보정의 각하결정)` 사본 열 개로 채워져서
>    두 팔이 겹칠 수가 없어. 첫 패스 융합 점수가 0.0164(=1/61)로 기준 0.0170에 걸려서
>    구조적으로 통과 불가야.
> 2. `어플 이름을 상표로 출원하려면 몇 류로 출원해야 하나요?` — 답은 맞는데 거의 같은 청크
>    여섯 개가 전달돼서 인용을 하나도 안 달아.
>
> 만들기 전에 먼저 재라. 중복이 실제로 얼마나 되는지, 어떤 기준(내용 해시 / 정규화 후 비교 /
> 임베딩 근접도)이 이 코퍼스에서 맞는지. 그리고 그 기준이 진짜 다른 조문을 지우지 않는지
> 확인해라 — 법령은 평행 조문이 많아서 문면이 비슷해도 다른 조문이야.
>
> 아래는 유지해야 한다:
> - 52문항 anchor 0.904, 재시도 0/52
> - crossref 25문항 anchor 0.880 (`--retry 3`)
> - statute 14문항 0.786
> - `지정상품` 5/5 응답
> - 법 §36① dense 순위: 전체질문 2위 / 지정상품 9위 / 「적어야 하는 사항은?」 4위
>   (**9위를 주시해라. `CANDIDATE_LIMIT`이 10이라 한 칸 남았다.**)
>
> 모델이 개입하는 건 여러 번 돌려서 편차까지 보고해라. `scripts/check_chat.py`가 인용마다
> 청크 전문을 찍어주니 **세지 말고 읽어서** 판단해라.
>
> 값을 못 하면 넣지 말고 그렇게 보고해라.

### 그다음 후보

우선순위 순입니다.

1. **인용 선택** — `x06`(실용신안 + 특허법 §81의3 준용)에서 6번 중 6번 특허법 §81의3③을
   인용하지만, 4번은 디자인보호법 조문도 **같이** 인용합니다. 그리고 준용을 선언하는
   실용신안법 §20이 한 번도 도착하지 않습니다 — `"제80조, 제81조 … 를 준용한다"`가 조문
   번호 나열뿐이라 질문과 겹치는 어휘가 없어서입니다. 역방향 간선이 필요합니다
   (지금 `references.attach`는 인용하는 쪽 → 인용되는 쪽으로만 걷습니다).
2. **`statute` 그룹의 실패 3건** (`s02`, `s03`, `s14`) — 전부 sparse 팔이 무관한 청크로
   채워지는 것. 1번 작업과 원인이 같을 수 있습니다.
3. **`parent` 간선이 기록만 되고 아무 데도 안 읽힙니다.** 계층 전달에 쓰거나 지우거나.
4. **활용률 지표가 API에만 있고 화면에 없습니다.** 트레이스 화면에 노출.
5. **`p01`의 anchor가 STALE** — 이전부터 있던 것.

---

## 3. 반드시 지킬 규칙

이 프로젝트에서 실제로 사고가 나서 생긴 규칙들입니다. 새 세션에 그대로 전달하십시오.

### 배포

```bash
docker compose build backend worker
docker compose up -d --force-recreate --no-deps backend worker
```

`docker compose up -d --build`는 **이미지만 만들고 컨테이너를 안 바꾼 적이 두 번** 있습니다.
그리고 검증은 작업 트리가 아니라 **실행 중인 컨테이너**에 대고 합니다.

```bash
docker compose exec -T backend grep -c <심볼> /app/app/...
docker compose exec -T postgres psql -U mopan -d mopan -c "select version_num from alembic_version;"
```

`docker compose run --rm migrate`가 **옛 이미지를 조용히 실행한 적**이 있습니다.
마이그레이션은 위 SQL로 직접 확인하십시오.

Git Bash에서 컨테이너 내부 경로는 `MSYS_NO_PATHCONV=1`을 붙이지 않으면 뭉개집니다.

### 만들지 말 것

- **인터페이스 뒤에 아무것도 안 하는 껍데기.** `NoneReranker`가 인자를 그대로 돌려주면서
  네 군데 호출부에 꽂혀 있었고, 파이프라인이 몇 주 동안 완성된 것처럼 읽혔습니다.
  "끈다"는 것은 **호출 경로에 없다**는 뜻이지 널 객체를 두라는 뜻이 아닙니다.
- **없는 기능을 있다고 적는 화면·문서·설정.** 설정 화면이 존재하지 않는 재순위 모델을
  "점수를 매기는 대상"이라고 설명하고 있었습니다.
- **도메인 하드코딩.** 모판은 범용 플랫폼입니다. 특허는 예시 데이터입니다.
  청킹 전략 선택이 `[제N류/GNNNNNN]` 표지 개수로 자동 결정되고 있었는데, 다른 사람 문서에는
  아무 소용이 없었습니다. 도메인 지식은 **설정과 프리셋**으로, 코드가 아니라.

### 측정

- **만들기 전에 잰다.** 이 프로젝트에서 상수를 버그에 맞춰 튜닝했다가 결론이 뒤집힌 게
  세 번입니다. 가설이 기각되는 것도 결과입니다.
- **anchor만 보고 고르지 않는다.** 활용률(인용/전달)과 **근거가 실제로 주장을 뒷받침하는지**를
  같이 봅니다. `CANDIDATE_LIMIT` 20에서 모든 숫자 검사를 통과하면서 답을 지어낸 적이 있습니다.
- **인용은 세지 말고 읽는다.** `check_chat.py`가 300자에서 잘라 보여주는 바람에 "근거 없음"
  판정이 틀린 적이 있습니다(지금은 고쳐졌습니다).
- 평가 질문은 **사람이 실제로 칠 문장**이어야 합니다. 맨 키워드 금지.
- `pytest`는 한 번에 하나만. `-n auto` 금지. 자기 `TEST_DATABASE_URL`을 쓰고 끝나면 지웁니다.

### 안전

- `.env`와 API 키는 **읽지도, 출력하지도, 커밋하지도** 않습니다.
- `docker compose config` 금지 — 해석된 환경변수를 키까지 통째로 출력합니다.
- 테스트는 네트워크에 나가지 않습니다. LLM 호출은 provider seam에서 가짜로 대체합니다.
- 색인은 실제 돈이 듭니다. **재색인 전에 비용을 추정해서 말하고**, 끝나면 실제 비용을 보고합니다.
  (참고: 법령 8건 3,335청크 = $0.0102. 유사상품 심사기준 재색인 = 약 $0.03.)
- 문서를 지우기 전에 **id를 먼저 확인**합니다. 정리하다가 진짜 문서를 지운 적이 있습니다.

### 완료의 정의

**커밋 + 푸시 + 배포 + 실행 중인 컨테이너에서 검증**까지 끝나야 완료입니다.
디스크에만 있는 코드는 전달된 것이 아닙니다.

커밋 메시지는 한국어로, 무엇을 바꿨고 무엇을 쟀는지, 그리고 **하지 않은 것**을 `남은 것`
절에 적습니다. `git log`가 그 형식입니다.

---

## 4. 클론 후 첫 실행

```bash
git clone https://github.com/soonkun/MOPAN.git
cd MOPAN
git checkout management-screens

cp .env.example .env          # OPENAI_API_KEY 등을 채웁니다
docker compose up -d --build
docker compose run --rm migrate
```

`data/uploads/`는 저장소에 없습니다. 코퍼스는 다시 올려야 합니다.
법령 여덟 건은 `python scripts/fetch_statutes.py`로 **바이트 단위로 동일하게** 다시 받을 수
있습니다. 심사기준 PDF들은 지식재산처(kipo.go.kr)에서 받습니다 — 출처와 다운로드 경로는
`scripts/fetch_statutes.py`의 주석과 `docs/superpowers/specs/`에 적혀 있습니다.

관리자 계정은 첫 가입자가 됩니다(프로덕션 모드에서는 막혀 있습니다 — `auth/service.py:29`).

---

## 5. 읽을 순서

1. `README.md` — 무엇을 만들고 있는지
2. `docs/화면.md` — 화면별 캡처와 설명
3. `docs/superpowers/specs/2026-09-01-document-structure.md` — **문서 성격 축.**
   자기완결형(연구보고서·편람)과 참조의존형(법령·규정)은 RAG 접근이 아예 달라야 한다는,
   이 프로젝트의 핵심 통찰이 여기 있습니다
4. `docs/superpowers/specs/2026-08-31-retrieval-redesign.md` — 검색 파이프라인 설계
5. `docs/superpowers/plans/` — 각 작업의 측정 기록
6. `backend/app/retrieval/keyword_search.py`의 주석 — 한국어 토큰화가 왜 이렇게 되어 있는지
