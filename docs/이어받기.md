# 이어받기

다른 기기에서 클론해 작업을 이어갈 때 먼저 읽는 문서입니다. 2026-09-05 기준.

브랜치는 `management-screens`. `main`이 아니라 이 브랜치에서 이어갑니다.

---

## 1. 지금 상태

### 돌아가는 것

| 영역 | 상태 |
|---|---|
| 채팅·RAG | 배포됨 |
| 접기 융합 (같은 항·같은 표 섹션이 슬롯을 겹쳐 쓰지 않음) | 배포됨, `RETRIEVAL_COLLAPSE=true` 기본 |
| 문서 간 인용 그래프 (「법명」 스코프 → cross-doc 간선 363개, 역방향 걷기) | 배포됨 |
| 섹션 머리 전달 (`attach_section_heads`) | 배포됨 |
| 워크플로우 전체 화면 편집기 (`/workflows/[id]`: 캔버스·서랍·인스펙터) | 배포됨 |
| 관리 화면 (분류·사용자·프롬프트·MCP·고급 설정·문서) | 배포됨 |
| 문서 성격 자동 판정 + 계층 청킹 | 배포됨 |
| 법령 여덟 건 색인 | 배포됨 (2026-09-05 재적재) |
| MCP 자동 사용 (서버 토글 = 기본 사용 설정, read만, 1회 숙고) | 배포됨, E2E 실측 |
| 생활정보 MCP (동봉: 날씨·환율·공휴일, 무키) | 배포됨 + 등록됨(risk=read) |

마이그레이션 head는 `0015` (chunk_edges.dst가 SET NULL - 대상 재적재가 간선 행을 지우지 않음).

### 코퍼스 (라이브 DB)

이전과 같음 + 법령 8건은 재적재됨. `sparse_lexeme_df` 표(파생 통계, `scripts/build_lexeme_df.py`)가 있음.

특허·실용신안 심사기준의 `self_contained` **재정의**는 그대로입니다. 되돌리지 마십시오.

### 배포 설정

`app_settings`: `RETRIEVAL_TOP_N 8 / RETRIEVAL_CANDIDATE_LIMIT 10 / WEAK_EVIDENCE_RRF_SCORE 0.0170 / QUERY_EXPANSION_COUNT 3(재시도 전용)`.
`CANDIDATE_LIMIT`은 10에서 올리지 마십시오(측정 기록: 20에서 날조).
env 전용 신규: `RETRIEVAL_COLLAPSE=true`(기본), `SPARSE_DF_TRIM=0.0`(꺼짐 - 켜기 전에 `build_lexeme_df.py`).

### 현재 지표 (--collapse는 배포 기본이라 그냥 돌리면 반영됨)

| 그룹 | n | recall | anchor | 비고 |
|---|---|---|---|---|
| 86문항 ALL | 86 | 0.802 | **0.884** | 이전 0.872 |
| crossref | 25 | 0.680 | **0.920** | 이전 0.880 - 접기 융합의 공로 |
| statute | 14 | 0.786 | 0.786 | 유지. 되묻기 7/14 |
| proviso | 10 | 0.800 | 1.000 | 유지 |
| colloquial (구어체) | 10 | 0.500 | **0.500** | 이전 0.100. `--retry 3`이면 0.600 |

```bash
docker compose cp scripts backend:/app/scripts   # 스크립트는 이미지에 없음
docker compose exec -T -w /app/scripts backend python /app/scripts/eval_retrieval.py
docker compose exec -T -w /app/scripts backend python /app/scripts/eval_retrieval.py --fixture /app/scripts/eval_questions_ko_colloquial.json
# --collapse off | on:CAP:OVERFETCH:jo|hang 으로 접기 변형 스윕
```

### 이번 회차(2026-09-05)에 결정된 것 - 근거는 커밋 메시지와 코드 주석에

- **접기 융합은 "접기만"이다.** 표 합산(cap>=2)·깊이 늘리기(overfetch)·조 단위
  접기는 전부 측정에서 기각 - `app/retrieval/collapse.py` 도크스트링의 표.
- **문서 간 해소의 동일성은 파일명이다.** "특허법 시행규칙.html" -> 특허법시행규칙.
  같은 제목 둘이면 모호로 잇지 않는다. 재적재 경쟁은 SET NULL(0015) + 삽입 전
  dst 검증 + `relink_external`로 막는다. **법령 재적재는 순차로.**
- **분류표 구어체 sparse 확장(Doc2Query)과 흔한 토큰 잘라내기(DF trim)는 기각.**
  둘 다 anchor 무변동 - 기각 사유와 재도전 조건은 `scripts/expand_table_sparse.py`
  도크스트링. 구어체를 움직인 건 전달(`attach_section_heads`)이었다.
- **워크플로우 편집기의 저장 계약은 안 바뀌었다.** 새 UI는 프론트만.
- **의도 게이트(INTENT_GATE, 기본 켜짐).** 인사말이 RAG를 타던 것을 검색 앞의
  분류 한 번으로 갈라냈다(app/chat/intent.py). 측정: 픽스처 96문항 전부 search,
  대화형 14종 전부 chat, 2회 반복 동일. 실패는 전부 search로 강등.
- **이중 스크롤바의 근본 원인은 sr-only(absolute)였다.** positioned 조상이 없어
  컨테이닝 블록이 뷰포트가 되면서 main의 클리핑을 탈출, 문서를 늘렸다(실측
  /prompts 1448px·/settings 1737px). main과 사이드바 스크롤 영역에 relative -
  전 페이지 docScrollH=900 확인.
- **브랜딩(0016 branding 싱글턴)과 계정 창.** 제목·대문 문구·추천 질문·마스코트가
  관리자 값이 됐고, 테마·로그아웃·닉네임·계정 삭제(비활성화+익명화, 마지막 관리자
  거부)는 사이드바 계정 창으로. 오른쪽 위 테마 순환 버튼(ThemeToggle)은 삭제.
- **MCP는 클로드 데스크톱의 모양이다(소유자 지시).** `+` 메뉴의 서버 단위 토글이
  "기본 사용 설정"(localStorage `mopan.mcp-servers-off`, 기본 켬)이고, 켜져 있으면
  `ChatRequest.auto_tool_ids`로 실려 `app/mcp/auto.py`가 1회 숙고로 알아서 부른다.
  **자동 경로는 read 등급만** - write·destructive는 사람의 결정(@, 승인 게이트)을
  지난다. `@`는 꺼져 있어도 항상 부른다(load_tool_calls, 한 줄도 안 겹침). 낡은
  id는 서버가 조용히 거른다(브라우저 기본 설정이라 섞이는 게 정상). 숙고는
  **의도와 무관하게** 돈다: "오늘 날씨 어때?"는 게이트가 chat으로 분류하지만
  도구가 근거를 내면 search로 승격된다(실사고에서 온 규칙, router.py 주석).
  UI는 `+` 메뉴 > 도구 설정 서브시트(서버가 늘어도 메뉴가 안 자람), 요약 값
  "N/M 켬".
  필수 인자가 질문에 없으면 숙고가 "ask: <되물음>"을 적고 그 문장이 그대로
  답이 된다(prompt_name=tool_clarify, 경고 배너 억제) - "오늘날씨 알려줘"가
  두 번 다 pass로 새던 실사고(trace 실측). 되물음의 답("대전")은 숙고에 실리는
  직전 대화 6턴으로 해석된다. 라이브 왕복 실측 통과.
- **사용자의 "지금"이 프롬프트에 실린다(app/core/localtime.py).** "올해 휴일"이
  2023년으로 답하던 실사고 - 모델의 올해는 학습 시점에 고정된다. 브라우저가
  `client_tz`(Intl 시간대)를 보내고, 서버가 그 시계의 날짜·시각 한 줄을 자동
  도구 숙고와 답변 프롬프트 양쪽에 덧붙인다(닉네임과 같은 주입 규칙). 없거나
  이상한 값은 DEFAULT_TIMEZONE(기본 Asia/Seoul, env)으로 강등.
- **외부 공개는 compose의 tunnel 서비스다(cloudflared Quick Tunnel).** 세션이
  띄운 프로세스는 메모리 압박 때 회수되던 실사고에서 왔다 - Docker가 관리하니
  죽어도 자동 재기동된다. 주소는 `docker compose logs tunnel | grep
  trycloudflare` (재시작마다 바뀜 - 고정 주소는 Named Tunnel 토큰으로, compose
  주석 참조). 컨테이너망에선 QUIC이 안 되어 `--protocol http2` 고정(실측).
- **생활정보 MCP를 동봉했다(mcp-examples 서비스).** "실제로 도움이 되는 MCP" -
  공개 원격 MCP는 무인증이 드물고 법령정보 MCP는 law.go.kr OC(이메일 ID 가입)가
  필요해서, 무키 공공 API 3종(open-meteo 날씨 / frankfurter 환율 / Nager.Date
  공휴일)을 backend 이미지로 감쌌다(`app/examples_mcp/main.py`). 그래서
  compose가 `MCP_ALLOW_PRIVATE_NETWORKS`를 기본 true로 넘긴다(등록은 관리자
  전용이라 완화 범위가 좁다 - .env로 끌 수 있음). deepwiki는 소유자 불만으로
  삭제. 실측 함정 둘: open-meteo 지오코딩은 한글 미색인(별칭 표로 우회),
  frankfurter.app은 301(follow_redirects).
- **사용자 관리에 관리자 비밀번호 재설정이 생겼다.** 이메일 발송이 없는
  시스템이라 "비밀번호 찾기"의 유일한 출구다. 임시값은 응답에 딱 한 번, 세션
  즉시 폐기, 자기 자신은 409(계정 설정이 정도). + 검색·닉네임 표시.
- **placeholder 규약: 예시·기본값은 "예) " 접두어 + 연하게(opacity 60%).**
  실값과 예시가 같은 진하기로 보인다는 소유자 지적. `.field`가 전역으로 처리.
- **전송 후 포커스는 입력창으로 돌아온다.** 첫 전송의 /chat -> /chat/{id} 전환이
  컴포넌트를 갈아 끼우며 포커스를 죽이던 것 - 마운트·대화 전환·전송 시 복원.
- **이중 스크롤바 3차(채팅 편).** 대화 스크롤 영역이 positioned가 아니라 안의
  sr-only(absolute)가 클리핑을 탈출, root를 늘려 main이 스크롤(실측 1030->2061,
  질문마다 성장). 스크롤러에 relative 한 단어 - main·사이드바에 이어 세 번째
  같은 기전이므로, **overflow 스크롤 영역을 만들면 relative를 같이 줄 것.**
- **GPT-5.6 Terra/Luna + 추론 수준.** ANSWER_MODELS에 추가(.env 마지막 정의),
  /api/models가 reasoning 플래그를 실어 + 메뉴에 "추론 수준" 행(최소/낮음/중간/
  높음, localStorage mopan.reasoning-effort). 적응은 프로바이더 한 곳: 추론
  계열은 temperature 제거+effort를 extra_body로(구 SDK가 명명 인자를 모름,
  실측 TypeError), 비추론 계열은 effort 폐기.
- **사례 질문("학회 발표 후 출원")의 사슬 전체를 실측으로 고쳤다.** 픽스처에
  s15(제30조 gold)로 영구화. ① 일괄 질의 재작성(RETRIEVAL_RECAST)은 **기각**
  (구어체 0.500/0.600 -> v1 0.400/0.300, v2 0.400/0.500 - recast.py 도크스트링).
  ② 진짜 원인 둘: 약근거 재시도의 mini 확장이 일반어만 생성(luna는 3/3
  공지예외·신규성 상실 생성 - QUERY_EXPANSION_RETRY_MODEL=gpt-5.6-luna, 재시도
  전용이라 게이트 지연 없음), 그리고 **되묻기 분기의 근거 절단**(정답이 9~14위에
  있는데 상위 3개만 봄 - clarify_evidence_items 3->14, "약근거란 순위가 뒤집혀
  있다는 뜻"). ③ clarify_agent v2: 사례 서술엔 결정적인 빠진 사실 하나를
  상담사처럼 되묻는다. 라이브 왕복 실측: "언제 발표하셨나요? 공개 시점에 따라
  공지예외 적용이 달라집니다" -> "지난달" -> 제30조 12개월·주장 절차·증명서류
  답변(인용 5).

---

## 2. 다음 작업 후보 (우선순위 순)

1. **구어체의 남은 절반은 검색 도달이다** (recall 0.500). 진짜 처방은 sparse 점수
   함수의 IDF - 형태소 토크나이저 + 파이썬 BM25 재점수(하네스에 kiwi 팔과 Bm25가
   이미 있다). bigram 위에서 DF는 희귀성 신호가 못 된다는 실측을 먼저 읽을 것
   (`scripts/expand_table_sparse.py` 기각 기록).
2. **되묻기 게이트와 구어체.** 근거가 도달해도(anchor 0.500) 라이브에선 8/10이
   되묻기로 샌다. `WEAK_EVIDENCE_RRF_SCORE`/`CLARIFY_ON_WEAK_EVIDENCE`는 오너 값.
   되묻기의 제안 질문 질은 눈에 띄게 좋아졌으므로 이대로가 나을 수도 있다.
3. **`statute` 그룹의 실패 3건**(s02·s03·s14) - sparse 팔이 무관한 청크로 채워짐.
   1번과 원인이 같다.
4. **relink가 간선만 되살리고 문서 화면의 해소 숫자는 옛 값**으로 남는다.
   documents.structure 갱신을 붙이거나, 화면에 "재계산" 버튼.
5. **`parent` 간선은 여전히 기록만 되고 안 읽힌다.** 쓰거나 지우거나.
6. **워크플로우 편집기 남은 것**: 미니맵·다중 선택·undo 없음(요청되면), 분기 빈
   조건이 서버에서 저장 허용됨, 핀치 줌 없음.
7. **활용률 지표가 API에만 있고 화면에 없음** / **p01 anchor STALE** - 이전부터.

---

## 3. 반드시 지킬 규칙

이 프로젝트에서 실제로 사고가 나서 생긴 규칙들입니다. 새 세션에 그대로 전달하십시오.

### 배포

```bash
docker compose build backend worker
docker compose up -d --force-recreate --no-deps backend worker
```

`docker compose up -d --build`는 **이미지만 만들고 컨테이너를 안 바꾼 적이 두 번** 있습니다.
그리고 검증은 작업 트리가 아니라 **실행 중인 컨테이너**에 대고 합니다.

```bash
docker compose exec -T backend grep -c <심볼> /app/app/...
docker compose exec -T postgres psql -U mopan -d mopan -c "select version_num from alembic_version;"
```

`docker compose run --rm migrate`가 **옛 이미지를 조용히 실행한 적**이 있습니다.
마이그레이션은 위 SQL로 직접 확인하십시오.

`docker compose cp`로 **기존 디렉터리에 디렉터리를 복사하면 안으로 중첩됩니다**
(`cp backend/app backend:/app/app` -> `/app/app/app`). 반드시 내용물 복사(`backend/app/.`)로.
실제로 이것 때문에 컨테이너가 옛 코드를 실행하면서 테스트가 헛돌았던 적이 있습니다.

Windows에서 backend 포트(8000)가 `bind: access permissions` 로 거부되면 Hyper-V
예약 범위에 들어간 것 - 관리자에서 `net stop winnat; net start winnat`.

Git Bash에서 컨테이너 내부 경로는 `MSYS_NO_PATHCONV=1`을 붙이지 않으면 뭉개집니다.

### 만들지 말 것

- **인터페이스 뒤에 아무것도 안 하는 껍데기.** `NoneReranker`가 인자를 그대로 돌려주면서
  네 군데 호출부에 꽂혀 있었고, 파이프라인이 몇 주 동안 완성된 것처럼 읽혔습니다.
  "끈다"는 것은 **호출 경로에 없다**는 뜻이지 널 객체를 두라는 뜻이 아닙니다.
- **없는 기능을 있다고 적는 화면·문서·설정.** 설정 화면이 존재하지 않는 재순위 모델을
  "점수를 매기는 대상"이라고 설명하고 있었습니다.
- **도메인 하드코딩.** 모판은 범용 플랫폼입니다. 특허는 예시 데이터입니다.
  청킹 전략 선택이 `[제N류/GNNNNNN]` 표지 개수로 자동 결정되고 있었는데, 다른 사람 문서에는
  아무 소용이 없었습니다. 도메인 지식은 **설정과 프리셋**으로, 코드가 아니라.

### 측정

- **만들기 전에 잰다.** 이 프로젝트에서 상수를 버그에 맞춰 튜닝했다가 결론이 뒤집힌 게
  세 번입니다. 가설이 기각되는 것도 결과입니다.
- **anchor만 보고 고르지 않는다.** 활용률(인용/전달)과 **근거가 실제로 주장을 뒷받침하는지**를
  같이 봅니다. `CANDIDATE_LIMIT` 20에서 모든 숫자 검사를 통과하면서 답을 지어낸 적이 있습니다.
- **인용은 세지 말고 읽는다.** `check_chat.py`가 300자에서 잘라 보여주는 바람에 "근거 없음"
  판정이 틀린 적이 있습니다(지금은 고쳐졌습니다).
- 평가 질문은 **사람이 실제로 칠 문장**이어야 합니다. 맨 키워드 금지.
- `pytest`는 한 번에 하나만. `-n auto` 금지. 자기 `TEST_DATABASE_URL`을 쓰고 끝나면 지웁니다.

### 안전

- `.env`와 API 키는 **읽지도, 출력하지도, 커밋하지도** 않습니다.
- `docker compose config` 금지 — 해석된 환경변수를 키까지 통째로 출력합니다.
- 테스트는 네트워크에 나가지 않습니다. LLM 호출은 provider seam에서 가짜로 대체합니다.
- 색인은 실제 돈이 듭니다. **재색인 전에 비용을 추정해서 말하고**, 끝나면 실제 비용을 보고합니다.
  (참고: 법령 8건 3,335청크 = $0.0102. 유사상품 심사기준 재색인 = 약 $0.03.)
- 문서를 지우기 전에 **id를 먼저 확인**합니다. 정리하다가 진짜 문서를 지운 적이 있습니다.

### 완료의 정의

**커밋 + 푸시 + 배포 + 실행 중인 컨테이너에서 검증**까지 끝나야 완료입니다.
디스크에만 있는 코드는 전달된 것이 아닙니다.

커밋 메시지는 한국어로, 무엇을 바꿨고 무엇을 쟀는지, 그리고 **하지 않은 것**을 `남은 것`
절에 적습니다. `git log`가 그 형식입니다.

---

## 4. 클론 후 첫 실행

```bash
git clone https://github.com/soonkun/MOPAN.git
cd MOPAN
git checkout management-screens

cp .env.example .env          # OPENAI_API_KEY 등을 채웁니다
docker compose up -d --build
docker compose run --rm migrate
```

`data/uploads/`는 저장소에 없습니다. 코퍼스는 다시 올려야 합니다.
법령 여덟 건은 `python scripts/fetch_statutes.py`로 **바이트 단위로 동일하게** 다시 받을 수
있습니다. 심사기준 PDF들은 지식재산처(kipo.go.kr)에서 받습니다 — 출처와 다운로드 경로는
`scripts/fetch_statutes.py`의 주석과 `docs/superpowers/specs/`에 적혀 있습니다.

관리자 계정은 첫 가입자가 됩니다(프로덕션 모드에서는 막혀 있습니다 — `auth/service.py:29`).

---

## 5. 읽을 순서

1. `README.md` — 무엇을 만들고 있는지
2. `docs/화면.md` — 화면별 캡처와 설명
3. `docs/superpowers/specs/2026-09-01-document-structure.md` — **문서 성격 축.**
   자기완결형(연구보고서·편람)과 참조의존형(법령·규정)은 RAG 접근이 아예 달라야 한다는,
   이 프로젝트의 핵심 통찰이 여기 있습니다
4. `docs/superpowers/specs/2026-08-31-retrieval-redesign.md` — 검색 파이프라인 설계
5. `docs/superpowers/plans/` — 각 작업의 측정 기록
6. `backend/app/retrieval/keyword_search.py`의 주석 — 한국어 토큰화가 왜 이렇게 되어 있는지
